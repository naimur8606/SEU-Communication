import { Helmet } from "react-helmet-async";

const Home = () => {
    return (
        <div>
            <Helmet>
                <title>SEUC | Home</title>
            </Helmet>
        </div>
    );
};

export default Home;