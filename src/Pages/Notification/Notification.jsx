import { Helmet } from "react-helmet-async";

const Notification = () => {
    return (
        <div>
            <Helmet>
                <title>SEUC | Notification</title>
            </Helmet>
        </div>
    );
};

export default Notification;